rootProject.name = "Wordle"
// This file satisfies the IDE's requirement for a Gradle build at the root.
// The actual Android build logic remains isolated in the /android directory
// to ensure Flutter's APK generation works correctly.
